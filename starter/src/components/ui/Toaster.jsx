import { ToastContainer } from "react-toastify";
import "react-toastify/dist/ReactToastify.css";

// Default export for the Toaster component
const Toaster = () => {
  return <ToastContainer />;
};

export default Toaster; // Use default export
